name: Build

on:
  workflow_call:
    inputs:
      push_image:
        description: 'Push docker image to registry'
        required: false
        default: false
        type: boolean

  workflow_dispatch:
    
  pull_request:
    branches: [ develop, production, feature/azure-deplop ]


jobs:
  core-build:
    name: Build codebase
    uses: ./.github/workflows/core-build.yml
    with:
      push_image: ${{ inputs.push_image }}
    secrets: inherit

  infra-build:
    name: Build infrastructure
    uses: ./.github/workflows/infra-build.yml
    secrets: inherit